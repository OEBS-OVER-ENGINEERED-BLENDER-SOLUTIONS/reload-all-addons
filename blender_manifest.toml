schema_version = "1.0.0"

# The unique identifier of the extension
id = "reload_all_addons"

# The version of the extension
version = "2.4.0"

# The name of the extension
name = "OEBS Addon Reloader"

# A short description (tagline) of the extension
tagline = "Reload Blender addons with full memory deep-clean"

# The author(s) of the extension
maintainer = "OEBS (Eric Mwangi)"

# The type of the extension
type = "add-on"

# A website related to the extension
website = "https://github.com/OEBS-OVER-ENGINEERED-BLENDER-SOLUTIONS/oebs-addon-reloader"

# Tags to help categorize the extension
tags = ["Development"]

# The minimum required Blender version
blender_version_min = "4.2.0"

# The license of the extension
license = [
  "SPDX:GPL-3.0-or-later",
]
